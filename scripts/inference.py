import numpy as np
import matplotlib.pyplot as plt
from src.models.rtdeter_wrapper import RTdeterWrapper
from src.models.yolo_wrapper import YoloWrapper

'''
The goal of inference.py is to provide visual evidence that under domain shift, RT-DETR localizes plates better than YOLO (or vice versa).
Ideally, we would want 1 or 2 pairs of source domain target domain samples with each model's prediction bounding box applied to showcase the
difference. 
'''








#def main():
    # TODO: 


#if __name__ == "__main__":
    #main()